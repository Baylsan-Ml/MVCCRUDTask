@model User

<div class="d-flex flex-column align-items-center">
    <h1 class="m-auto fs-3">Create a new user</h1>
</div>

<form method="post" action="/Users/Store">
   <div>
        <label name="Name">Name</label>
        <input type="text" name="Name" value="@Model.Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div>
        <label name="Age">Age</label>
        <input type="number" name="Age" value="@(Model.Age==0 ? "" : Model.Age)" class="form-control" />
        <span asp-validation-for="Age" class="text-danger"></span>
    </div>
    <div>
        <label name="City">City</label>
        <input type="text" name="City" value="@Model.City" class="form-control" />
        <span asp-validation-for="City" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-info mt-2">Create</button>
</form>